server:
    port:8081
eurecka:
    instance:
        hostname: localhost
        leaseRenewalIntervalInSeconds: 5
    client:
        registerWithEureka: false
        fetchRegistry: false
        serviceUrl:
            defaultZone: http://${eurecka.instance.hostname}:${server.port}/eurecka/